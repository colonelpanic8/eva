* Added: The text and voice models are selectable. The picker lists the models your own account can use and also accepts a typed name, so a new model works without an app update.
* Fixed: A bad key or an unusable text model is now reported when you connect, instead of appearing to connect and failing on the first message.
* Fixed: A rejected OpenAI request shows the provider's own message instead of the raw response body.
