* Fixed: Mova no longer appears twice under Extensions when both Mova's own extension and the catalog's Mova package are installed. Mova's extension handles the actions both offer, so the model sees each once; the package's remaining link actions are listed inside Mova's row.
