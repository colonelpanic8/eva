<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="com.colonelpanic.mova.permission.READ_TODOS" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <!-- Voice is the point of the app: there is no microphone-less mode to fall back to. -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />
    <uses-sdk tools:overrideLibrary="rikka.shizuku.api" />

    <queries>
        <provider android:authorities="com.colonelpanic.mova.provider;sh.paseo.assistant" />
        <intent>
            <action android:name="com.colonelpanic.eva.action.EXTENSION" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <package android:name="moe.shizuku.privileged.api" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Which installed apps offer to play a spoken request, so one can be named. -->
        <intent>
            <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
        </intent>
        <!-- Media apps that can be asked to play by name while cold, the way Android Auto asks them. -->
        <intent>
            <action android:name="android.media.browse.MediaBrowserService" />
        </intent>
        <!-- Media3 library apps that expose searchable items and accept queue additions. -->
        <intent>
            <action android:name="androidx.media3.session.MediaLibraryService" />
        </intent>
    </queries>

    <application
        android:name=".EvaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security"
        android:usesCleartextTraffic="false"
        tools:targetApi="24"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVA">
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="@bool/shizuku_enabled"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Hands the telephony stack one outgoing MMS at a time; grants are per-URI and short lived. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.mms"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/mms_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VOICE_COMMAND" />
                <action android:name="android.intent.action.VOICE_ASSIST" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- What a long-pressed headset button becomes: web search when open, hands free when locked. -->
            <intent-filter>
                <action android:name="android.speech.action.WEB_SEARCH" />
                <action android:name="android.speech.action.VOICE_SEARCH_HANDS_FREE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="com.android.systemui.action_assist_icon"
                android:resource="@mipmap/ic_launcher" />
        </activity>

        <!-- Receives Spotify's browser redirect and returns the existing EVA task to settings. -->
        <activity
            android:name=".SpotifyRedirectActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="spotify"
                    android:scheme="eva" />
            </intent-filter>
        </activity>

        <!-- Notification access supports media controls; message collection requires a separate
             in-app opt-in. -->
        <service
            android:name=".adapters.android.EvaNotificationListener"
            android:exported="true"
            android:label="@string/media_control_service_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".audio.VoiceSessionService"
            android:exported="false"
            android:foregroundServiceType="microphone|mediaPlayback|location"
            tools:targetApi="30" />

        <!-- Finishes a turn after its call ended. Short by design: a turn that cannot finish in
             the allowance is interrupted and said so, not left half-done in the dark. -->
        <service
            android:name=".conversation.TurnWorkService"
            android:exported="false"
            android:foregroundServiceType="shortService"
            tools:targetApi="34" />

        <!-- Holding the assistant role, rather than only answering ACTION_ASSIST, is what puts
             EVA behind the assist gesture and lets it answer over another app. Only the system
             holds BIND_VOICE_INTERACTION, so only the system can bind either service. -->
        <service
            android:name=".assist.EvaVoiceInteractionService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>

            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/interaction_service" />
        </service>

        <service
            android:name=".assist.EvaVoiceInteractionSessionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION" />

        <!-- Required of any voice interaction service, and made the device-wide recognizer when
             EVA is selected, so it has to stay usable by unrelated apps. EVA forwards instead of
             transcribing; see EvaRecognitionService. Callers bind it directly, so it cannot be
             permission-guarded. -->
        <service
            android:name=".assist.EvaRecognitionService"
            android:exported="true"
            android:label="@string/recognition_service_name"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.speech"
                android:resource="@xml/recognition_service" />
        </service>
    </application>
</manifest>
