{
  "claim": "A spoken request executes a phone action through the real controller and the model speaks the result",
  "device": "Android 16 API 36 google_apis_playstore x86_64 emulator (fresh headless AVD, no host audio)",
  "build": "debug package com.colonelpanic.eva.debug from commit after 321c9c9",
  "method": "NativeVoiceActionLiveTest replaces captured microphone samples with an espeak fixture; catalog, controller, dispatcher, journal, and Android intent backend are the production objects",
  "fixture": {
    "text": "Set a timer for three minutes.",
    "format": "48 kHz mono PCM16",
    "bytes": 439908,
    "seconds": 4.58
  },
  "observed": {
    "userTranscriptInJournal": "Set a timer for three minutes",
    "toolCall": "eva.android.timer.set with seconds=180, correlated to the delegated backend turn",
    "handoff": "Timer started.",
    "clockApp": "com.google.android.deskclock resumed with a running 3m timer",
    "spokenConfirmation": "Three-minute timer started.",
    "sessionAfterHandoff": "CONNECTED with media Connected after a 5 s dwell with Clock in the foreground",
    "foregroundService": "VoiceSessionService started with the app in TOP state; ActivityManager logged the FGS as allowed",
    "crashBuffer": "empty"
  },
  "runs": [
    {
      "test": "NativeVoiceActionLiveTest",
      "result": "PASS",
      "seconds": 11.3
    },
    {
      "test": "NativeVoiceActionLiveTest with 5 s dwell",
      "result": "PASS"
    },
    {
      "test": "NativeVoiceLiveTest (empty catalog, chat only)",
      "result": "PASS",
      "nonzeroDecodedBytes": 139775
    }
  ],
  "notVerified": [
    "physical phone (USB dropped mid-run and did not return)",
    "audible playback",
    "barge-in",
    "a second spoken action while another app is in front"
  ],
  "brokerTests": 18,
  "jvmTests": 63,
  "releasePackage": {
    "build": "v0.2.4 signed release APK from GitHub release, package com.colonelpanic.eva, installed on the same emulator",
    "observed": {
      "liveVoiceSession": "Voice connected; status shows gpt-live-1-codex with gpt-5.6-luna and the voice session copy",
      "foregroundService": "VoiceSessionService isForeground=true, types=0x80 (microphone), ongoing notification 'EVA is in a voice session'",
      "stopVoice": "service record and notification both gone after Stop voice; UI returned to the disconnected state"
    },
    "limit": "Synthetic speech cannot be injected into the release package, so the spoken-action round trip is proven on the debug package; the release package differs only by package ID and minification being off"
  },
  "obtainiumOnEmulator": {
    "obtainium": "v1.6.15 app-release.apk installed on the same API 36 emulator",
    "install": "EVA added from https://github.com/colonelpanic8/eva through the obtainium:// deep link; v0.2.4 eva.apk installed by Obtainium (installerPackageName=dev.imranr.obtainium) after allowing the unknown-app source; Play Protect prompted once and did not block",
    "assistantRole": "granted to com.colonelpanic.eva; the assist keyevent brought EVA to the foreground from any app",
    "typedAction": "'Open the wifi settings' handed off to com.android.settings Wi-Fi; EVA showed Handed off / Open a settings screen / Settings opened.",
    "liveVoice": "Voice connected with gpt-live-1-codex and gpt-5.6-luna shown; VoiceSessionService isForeground=true types=0x80; ongoing notification present; Stop voice removed both",
    "physicalPhone": "still absent from USB; nothing here was run on hardware"
  }
}
